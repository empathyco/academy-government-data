import { Filter } from '@empathyco/x-types';
import { CreateElement, VNode } from 'vue';
import FiltersInjectionMixin from './filters-injection.mixin';
declare const SortedFilters_base: import("vue-class-component/lib/declarations").VueClass<FiltersInjectionMixin>;
/**
 * Component that sorts a list of filters and returns them using the default scoped slot.
 *
 * @public
 */
export default class SortedFilters extends SortedFilters_base {
    /**
     * An array of filters with the selected filters at the beginning of the list.
     *
     * @returns Array of filters.
     * @internal
     */
    get sortedFilters(): Filter[];
    render(h: CreateElement): VNode;
}
export {};
//# sourceMappingURL=sorted-filters.vue?rollup-plugin-vue=script.d.ts.map