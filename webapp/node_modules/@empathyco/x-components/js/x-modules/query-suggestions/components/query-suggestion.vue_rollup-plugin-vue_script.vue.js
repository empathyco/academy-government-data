import { __decorate } from 'tslib';
import { Prop, Component, Vue } from 'vue-property-decorator';
import __vue_component__ from '../../../components/suggestions/base-suggestion.vue.js';
import { Getter } from '../../../components/decorators/store.decorators.js';
import { xComponentMixin } from '../../../components/x-component.mixin.js';
import { querySuggestionsXModule } from '../x-module.js';

/**
 * This component renders a suggestion for a query. A query suggestion is a recommended query
 * based on previous search queries. It contains the query itself and a set of filters associated.
 * For example, if you're searching for _shirt_, a query suggestion could be _long sleeve shirt_.
 *
 * @public
 */
let QuerySuggestion = class QuerySuggestion extends Vue {
    constructor() {
        super(...arguments);
        /**
         * Emits {@link QuerySuggestionsXEvents.UserSelectedAQuerySuggestion} with the suggestion as
         * payload when selecting the query suggestion.
         *
         * @internal
         */
        this.event = {
            UserSelectedAQuerySuggestion: this.suggestion
        };
    }
};
__decorate([
    Getter('querySuggestions', 'normalizedQuery')
], QuerySuggestion.prototype, "query", void 0);
__decorate([
    Prop({ required: true })
], QuerySuggestion.prototype, "suggestion", void 0);
QuerySuggestion = __decorate([
    Component({
        components: { BaseSuggestion: __vue_component__ },
        mixins: [xComponentMixin(querySuggestionsXModule)]
    })
], QuerySuggestion);
var script = QuerySuggestion;

export { script as default };
//# sourceMappingURL=query-suggestion.vue_rollup-plugin-vue_script.vue.js.map
