import { __decorate } from 'tslib';
import { Prop, Provide, Component } from 'vue-property-decorator';
import Vue from 'vue';
import { State } from '../../../components/decorators/store.decorators.js';
import { xComponentMixin } from '../../../components/x-component.mixin.js';
import { identifierResultsXModule } from '../x-module.js';

/**
 * Paints the list of identifier results stored in the state. Each identifier result should be
 * represented by a {@link IdentifierResult | identifier result component} besides any
 * other component.
 *
 * @public
 */
let IdentifierResults = class IdentifierResults extends Vue {
    constructor() {
        super(...arguments);
        /**
         * The additional events to be emitted by the mandatory {@link BaseResultLink} component.
         *
         * @public
         */
        this.resultClickExtraEvents = [
            'UserClickedAIdentifierResult'
        ];
    }
};
__decorate([
    Prop({ default: 'ul' })
], IdentifierResults.prototype, "animation", void 0);
__decorate([
    State('identifierResults', 'identifierResults')
], IdentifierResults.prototype, "identifierResults", void 0);
__decorate([
    Provide()
], IdentifierResults.prototype, "resultClickExtraEvents", void 0);
IdentifierResults = __decorate([
    Component({
        mixins: [xComponentMixin(identifierResultsXModule)]
    })
], IdentifierResults);
var script = IdentifierResults;

export { script as default };
//# sourceMappingURL=identifier-results.vue_rollup-plugin-vue_script.vue.js.map
