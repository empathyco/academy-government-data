import { __decorate } from 'tslib';
import Vue from 'vue';
import { Prop, Component } from 'vue-property-decorator';
import __vue_component__ from '../../base-rating.vue.js';

/**
 * Renders a label for a rating filter, allowing to override the elements used to paint
 * the rating.
 *
 * @public
 */
let BaseRatingFilterLabel = class BaseRatingFilterLabel extends Vue {
    /**
     * Converts the label string into a number.
     *
     * @returns The label as number or 0 if it is not a valid number.
     *
     * @internal
     * */
    get value() {
        const value = parseFloat(this.filter.label) ?? 0;
        return Number.isNaN(value) ? 0 : value;
    }
};
__decorate([
    Prop({ required: true })
], BaseRatingFilterLabel.prototype, "filter", void 0);
__decorate([
    Prop({ default: 5 })
], BaseRatingFilterLabel.prototype, "max", void 0);
BaseRatingFilterLabel = __decorate([
    Component({
        components: {
            BaseRating: __vue_component__
        }
    })
], BaseRatingFilterLabel);
var script = BaseRatingFilterLabel;

export { script as default };
//# sourceMappingURL=base-rating-filter-label.vue_rollup-plugin-vue_script.vue.js.map
