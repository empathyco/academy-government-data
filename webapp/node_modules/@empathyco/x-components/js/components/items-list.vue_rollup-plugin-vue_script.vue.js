import { __decorate } from 'tslib';
import Vue from 'vue';
import { Prop, Component } from 'vue-property-decorator';
import { toKebabCase } from '../utils/string.js';

/**
 * It renders a list of {@link ListItem} providing a slot for each `slotName` which depends on
 * the `modelName`of the item.
 *
 * @public
 */
let ItemsList = class ItemsList extends Vue {
    /**
     * The list of the items with additional properties.
     *
     * @returns A list of items with `dataTest`, `class` and the `slotName` for each item.
     *
     * @internal
     */
    get computedItems() {
        return this.items.map(item => {
            const modelName = toKebabCase(item.modelName);
            return {
                ...item,
                dataTest: `${modelName}s-list-item`,
                class: [`x-${modelName}s-list-item`],
                slotName: modelName
            };
        });
    }
};
__decorate([
    Prop({ default: 'ul' })
], ItemsList.prototype, "animation", void 0);
__decorate([
    Prop({ required: true })
], ItemsList.prototype, "items", void 0);
ItemsList = __decorate([
    Component
], ItemsList);
var script = ItemsList;

export { script as default };
//# sourceMappingURL=items-list.vue_rollup-plugin-vue_script.vue.js.map
