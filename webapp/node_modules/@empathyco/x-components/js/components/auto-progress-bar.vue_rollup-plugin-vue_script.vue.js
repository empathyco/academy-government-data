import { __decorate } from 'tslib';
import Vue from 'vue';
import { Prop, Component } from 'vue-property-decorator';

/**
 * An auto progress bar component.
 *
 * @public
 */
let AutoProgressBar = class AutoProgressBar extends Vue {
    /**
     * Computed property to calculate the animation's duration.
     *
     * @returns The CSS styles of the animation.
     *
     * @internal
     */
    get cssStyles() {
        return { animationDuration: `${this.durationInSeconds}s` };
    }
};
__decorate([
    Prop({ default: true })
], AutoProgressBar.prototype, "isLoading", void 0);
__decorate([
    Prop({ default: 5 })
], AutoProgressBar.prototype, "durationInSeconds", void 0);
AutoProgressBar = __decorate([
    Component
], AutoProgressBar);
var script = AutoProgressBar;

export { script as default };
//# sourceMappingURL=auto-progress-bar.vue_rollup-plugin-vue_script.vue.js.map
