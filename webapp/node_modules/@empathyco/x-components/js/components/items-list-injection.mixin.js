import { __decorate } from 'tslib';
import Vue from 'vue';
import Component from 'vue-class-component';
import { XProvide, XInject } from './decorators/injection.decorators.js';
import { LIST_ITEMS_KEY } from './decorators/injection.consts.js';

/**
 * Mixin to facilitate providing and injecting a list of list items. Injected list is
 * available at `injectedListItems`, and the provided list should be stored in `items`.
 *
 * @public
 */
let ItemsListInjectionMixin = class ItemsListInjectionMixin extends Vue {
    /**
     * The computed list items of the entity that uses the mixin.
     *
     * @remarks It should be overridden in the component that uses the mixin and it's intended to be
     * filled with items from the state. Vue doesn't allow mixins as abstract classes.
     * @returns An empty array as fallback in case it is not overridden.
     * @internal
     */
    get items() {
        return [];
    }
};
__decorate([
    XProvide(LIST_ITEMS_KEY)
], ItemsListInjectionMixin.prototype, "items", null);
__decorate([
    XInject(LIST_ITEMS_KEY)
], ItemsListInjectionMixin.prototype, "injectedListItems", void 0);
ItemsListInjectionMixin = __decorate([
    Component
], ItemsListInjectionMixin);

export { ItemsListInjectionMixin };
//# sourceMappingURL=items-list-injection.mixin.js.map
