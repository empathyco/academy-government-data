import { __decorate } from 'tslib';
import Vue from 'vue';
import { Prop } from 'vue-property-decorator';
import Component from 'vue-class-component';

/**
 * Mixin to share layouts logic.
 *
 * @public
 */
let LayoutsMixin = class LayoutsMixin extends Vue {
    /**
     * Function to check if an slot has rendered content or not.
     *
     * @param slotNames - A VNode Array with of each slot.
     * @returns True if the slot has rendered content or false otherwise.
     *
     * @internal
     */
    hasContent(...slotNames) {
        return ((this.devMode ||
            slotNames.some(slotName => this.$slots[slotName]?.some(vNode => vNode.tag !== undefined))) ??
            false);
    }
};
__decorate([
    Prop({ default: false })
], LayoutsMixin.prototype, "devMode", void 0);
LayoutsMixin = __decorate([
    Component
], LayoutsMixin);
var LayoutsMixin$1 = LayoutsMixin;

export { LayoutsMixin$1 as default };
//# sourceMappingURL=layouts.mixin.js.map
