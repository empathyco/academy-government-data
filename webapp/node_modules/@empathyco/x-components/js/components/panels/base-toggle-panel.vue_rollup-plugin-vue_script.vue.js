import { __decorate } from 'tslib';
import Vue from 'vue';
import { Prop, Component } from 'vue-property-decorator';

/**
 * Simple panel that receives its open state via prop, which is responsible of rendering
 * default slot inside a configurable transition.
 *
 * @public
 */
let BaseTogglePanel = class BaseTogglePanel extends Vue {
};
__decorate([
    Prop({ required: true })
], BaseTogglePanel.prototype, "open", void 0);
__decorate([
    Prop({ default: 'div' })
], BaseTogglePanel.prototype, "animation", void 0);
BaseTogglePanel = __decorate([
    Component
], BaseTogglePanel);
var script = BaseTogglePanel;

export { script as default };
//# sourceMappingURL=base-toggle-panel.vue_rollup-plugin-vue_script.vue.js.map
