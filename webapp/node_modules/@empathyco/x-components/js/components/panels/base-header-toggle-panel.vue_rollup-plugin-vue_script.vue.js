import { __decorate } from 'tslib';
import Vue from 'vue';
import { Prop, Component } from 'vue-property-decorator';
import { NoElement } from '../no-element.js';
import __vue_component__ from './base-toggle-panel.vue.js';

/**
 * Toggle panel which uses the base toggle panel, adds a header and manage the
 * open / close state of the panel.
 *
 * @public
 */
let BaseHeaderTogglePanel = class BaseHeaderTogglePanel extends Vue {
    constructor() {
        super(...arguments);
        /**
         * Handles if the base panel is open or closed.
         *
         * @internal
         */
        this.open = !this.startCollapsed;
    }
    /**
     * Toggles the open property.
     *
     * @internal
     */
    toggleOpen() {
        this.open = !this.open;
        this.emitOpenStatusEvent();
    }
    /**
     * Emits open status event.
     *
     * @internal
     */
    emitOpenStatusEvent() {
        this.$emit(this.open ? 'open' : 'close');
    }
};
__decorate([
    Prop({ default: () => NoElement })
], BaseHeaderTogglePanel.prototype, "animation", void 0);
__decorate([
    Prop({ default: false })
], BaseHeaderTogglePanel.prototype, "startCollapsed", void 0);
BaseHeaderTogglePanel = __decorate([
    Component({
        components: { BaseTogglePanel: __vue_component__ }
    })
], BaseHeaderTogglePanel);
var script = BaseHeaderTogglePanel;

export { script as default };
//# sourceMappingURL=base-header-toggle-panel.vue_rollup-plugin-vue_script.vue.js.map
