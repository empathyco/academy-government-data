import { Filter } from '@empathyco/x-types';
import { Dictionary } from '../../../../types';
import { Mapper, RequestMapper, RequestMapperContext } from '../../../empathy-adapter.types';
/**
 * TODO https://searchbroker.atlassian.net/browse/EX-2163
 *
 * @public
 */
export declare class EmpathyRequestFiltersMapper implements Mapper<Dictionary<Filter[]>, string[]> {
    private readonly mapFiltersValue;
    constructor(filtersValueMappers: RequestMapper<Filter[], string[]>[]);
    map(selectedFilters: Dictionary<Filter[]>, filtersValue: string[], context: RequestMapperContext): string[];
}
