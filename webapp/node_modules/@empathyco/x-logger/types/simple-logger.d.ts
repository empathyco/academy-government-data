import { Logger, LogLevel } from './logger.interfaces';
export declare class SimpleLogger implements Logger {
    private static commonConsoleLevel;
    private static commonServerLevel;
    private readonly instanceTags;
    constructor(...args: any[]);
    set consoleLevel(level: LogLevel);
    get consoleLevel(): LogLevel;
    set serverLevel(level: LogLevel);
    get serverLevel(): LogLevel;
    error(...args: any[]): void;
    warn(...args: any[]): void;
    info(...args: any[]): void;
    debug(...args: any[]): void;
    trace(...args: any[]): void;
    child(...args: any[]): Logger;
    private log;
    private sendLogToConsole;
    private sendLogToServer;
}
